# Placeholder package.
#
# This is where the provider-agnostic LLM wrapper will live (next step,
# item #4 in the project's next-steps list): a common interface that hides
# whether a given call goes to Groq (live interviewer) or Gemini (grading).
